@model IEnumerable<Resume.Models.Entities.Education>

@{
    ViewData["Title"] = "Təhsil";
    Layout = "~/Areas/Admin/Views/Shared/_AdminLayout.cshtml";
}


<p>
    <a asp-action="Create" class="btn btn-outline-success">Əlavə Et</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                Təhsil Mərkəzi
            </th>
            <th>
                Başlama vaxtı
            </th>
            <th>
                Bitirmə vaxtı
            </th>
            <th>
                Fakultə
            </th>
            <th>
                İxtisas
            </th>
            <th>
                Açıqlama
            </th>
            <th>
                Tarix tipi
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @item.EducationCenter
                </td>
                <td style="width:120px">
                    @item.StartDate.ToString("dd-MM-yyyy")
                </td>
                <td style="width:120px">
                    @if (item.Present)
                    {
                        <span>dəvam edir</span>
                    }
                    else
                    {
                        @item.EndDate.ToString("dd-MM-yyyy")
                    }
                </td>
                <td>
                    @item.Faculty
                </td>
                <td>
                    @item.Specialty
                </td>
                <td>
                    @item.Description
                </td>
                <td style="width:100px">
                    @if (item.ShowDay)
                    {
                        <span>Gün.</span>
                    }
                    @if (item.ShowMonth)
                    {
                        <span>Ay.</span>
                    }
                    @if (item.ShowYear)
                    {
                        <span>İl</span>
                    }
                </td>
                <td style="width:70px">
                    <a asp-action="Edit" asp-route-id="@IDAncryption.Encrypt(item.ID.ToString())"><i class="fas fa-edit test" style="color:green"></i></a> |
                    <a asp-action="Delete" asp-route-id="@IDAncryption.Encrypt(item.ID.ToString())" title="Sil"><i class="fas fa-trash-alt" style="color:red"></i></a>
                </td>
            </tr>
        }
    </tbody>
</table>
