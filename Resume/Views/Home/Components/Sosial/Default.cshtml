@model IEnumerable<Resume.Models.Entities.Sosial>

    <div class="links">
        @foreach (var item in Model)
        {
        <a href="@item.PageURL">
            <i class="fab fa-github text-primary" style="font-size:35px"></i>
        </a>
        }
    </div>